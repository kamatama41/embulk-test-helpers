machine:
  java:
    version: oraclejdk8

deployment:
  snapshot:
    branch: /^(?!(master|release|gh-pages|snapshot)$).+$/
    commands:
      - ./release_snapshot.sh
#  release:
#    branch: master
#    commands:
#      - ./release.sh release
