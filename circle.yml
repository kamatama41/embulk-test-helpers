machine:
  java:
    version: oraclejdk8

deployment:
  snapshot:
    branch: /^(?!(master|release|gh-pages|snapshot)$).+$/
    commands:
      - rm -rf ./repository
      - ./release.sh snapshot
  release:
    branch: master
    commands:
      - ./release.sh release
