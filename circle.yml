machine:
  java:
    version: oraclejdk8

deployment:
  snapshot:
    branch: /^(?!(master|gh-pages|snapshot)$).+$/
    commands:
      - ./release_snapshot.sh
  release:
    branch: gh-pages
    commands:
      - ./gradlew release -Prelease.useAutomaticVersion=true
